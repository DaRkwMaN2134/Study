# Ревью проекта: [SAPR-Калькулятор]

## Что сделано хорошо

Классы разбиты по принципу S из SOLID

Классы используют интерфейсы для централизации

Добавить новую фигуру относительно легко, нужно будет написать дополнительно к ней калькулятор, логирование и создание, но ошибок и конфликтов с другими фигурами она не вызовет, поэтому внедрить + - легко

Методы работают в контексте определенных фигур и методов

Больших методов с миллионом задач нет, принцип S соблюдается

Ошибки обрабатываются

Логирование есть

Входные данные проверяются

Есть тесты

DI для замены зависимостей в тестах используются

Бесконечных циклов нет

Нет явных утечек памяти 

Асинхронные методы используются

## Что можно улучшить
1.Есть полиморфизм в каждом классе у методов Area/Perimeter/PrintInfo/Draw
2.Комментарии есть, но они маленькие и малоинформативные
3.Для тестов более конкретных тестов можно создать пустышки, но проблем с этим вроде как нет

## Как улучшить (словами)
1.Можно было бы вынести общую логику отдельно 
2.Добавить комментарии
3.Создать эти пустышки, но опять же проблем в этим нет